# AQ Fund

Shiny app for AQ Fund analytics. This is a development app, not the current public explorer.

Restore packages from this folder's `renv.lock` (R 4.3.3) with `renv::restore()`.

Do not commit `rsconnect/` deployment records.
